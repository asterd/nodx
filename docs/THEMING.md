# Theming

> Authoritative theming reference moved to
> [`docs/reference/themes.md`](./reference/themes.md). That page covers
> built-in themes, design tokens, the safe NODS subset, the component
> selector table, package-level themes, and front-matter components.

The source `.nods` files for the built-in themes live under
[`docs/themes/`](./themes/).
