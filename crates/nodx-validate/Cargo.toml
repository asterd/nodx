[package]
name = "nodx-validate"
version = "1.0.0"
edition = "2024"
license = "MIT"
description = "NODX 1.0 semantic validator, profile resolver, and stable diagnostic JSON"

[lib]
name = "nodx_validate"
path = "src/lib.rs"

[dependencies]
nodx-core = { path = "../nodx-core" }
nodx-style = { path = "../nodx-style" }
nodx-url = { path = "../nodx-url" }
