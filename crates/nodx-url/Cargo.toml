[package]
name = "nodx-url"
version = "1.0.0"
edition = "2024"
license = "MIT"
description = "NODX 1.0 URL and package-path classification policy with no I/O"

[lib]
name = "nodx_url"
path = "src/lib.rs"

[dependencies]
nodx-core = { path = "../nodx-core" }
