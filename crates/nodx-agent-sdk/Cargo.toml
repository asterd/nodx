[package]
name = "nodx-agent-sdk"
version = "1.0.0"
edition = "2024"
license = "MIT"
description = "NODX 1.0 Agent Mutate SDK: validated, hash-checked, atomically rolled-back mutations"

[lib]
name = "nodx_agent_sdk"
path = "src/lib.rs"

[dependencies]
nodx-core = { path = "../nodx-core" }
nodx-ncp = { path = "../nodx-ncp" }
nodx-validate = { path = "../nodx-validate" }
