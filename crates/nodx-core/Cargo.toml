[package]
name = "nodx-core"
version = "1.0.0"
edition = "2024"
license = "MIT"
description = "NODX 1.0 reference AST, parser, canonical JSON, navigation graph, and resource limits"

[lib]
name = "nodx_core"
path = "src/lib.rs"
