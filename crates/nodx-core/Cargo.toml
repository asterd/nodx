[package]
name = "nodx-core"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Minimal NODX 0.1 reference parser, canonicalizer, and renderers"

[lib]
name = "nodx_core"
path = "src/lib.rs"

[dependencies]
nodx-package = { path = "../nodx-package" }
nodx-url = { path = "../nodx-url" }
