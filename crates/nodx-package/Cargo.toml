[package]
name = "nodx-package"
version = "1.0.0"
edition = "2024"
license = "MIT"
description = "NODX 1.0 safe read-only package reader (stored + deflate ZIP) with manifest verification"

[lib]
name = "nodx_package"
path = "src/lib.rs"

[dependencies]
nodx-core = { path = "../nodx-core" }
nodx-url = { path = "../nodx-url" }
