# Playground examples

These files are the source documents loaded by `apps/web`.

Keep playground-facing examples in this directory so the browser sample
selector, the published GitHub Pages artifact, and the regression tests all
point at the same source set. Root-level files in `examples/` are broader
repository fixtures used by docs, conformance scripts, and package tests.
